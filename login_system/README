This folder contains a very basic example for OpenID integration.
Remeber you need to create the database first:

$ python
>>> import example
>>> example.init_db()

Before running the project.

To login to the system use an openid which you have access to or use the following admin account:

Username: 'admin'
Password: 'password'
